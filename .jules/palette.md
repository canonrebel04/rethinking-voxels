## 2024-06-29 - Missing UI Options
**Learning:** Orphaned `.lang` strings often indicate a setting that needs to be added to the property tree in `shaders.properties` to be accessible to users.
**Action:** When auditing UI menus for completeness, check for `.lang` entries that aren't hooked up in the screen configuration so users can access them.
