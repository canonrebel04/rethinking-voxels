## 2024-05-15 - Missing Lang Settings UI integration
**Learning:** Orphaned `.lang` strings indicate a UI setting that needs to be added to the property tree in `shaders.properties` so users can access them.
**Action:** Add the missing screen mapping and settings to `shaders.properties`.
