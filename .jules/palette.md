## 2024-06-25 - Missing Lang Entries Break UX
**Learning:** Missing `.lang` entries for options defined in `shaders.properties` result in raw variable names and missing tooltips in the UI, degrading accessibility and user experience.
**Action:** Always cross-reference `shaders.properties` with `.lang` files when adding or reviewing options to ensure consistent, readable UI text.
